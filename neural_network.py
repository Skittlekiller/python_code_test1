#Neural Network Implementation

#structure of the network
#single neuron layer with x inputs
#y neurons  ( may be reduced through trimming)
#z outputs ( start with binary)

def input_layer(x):
	#here is the input layer list
	if x.type = list:
		neuron_layer(x)

def neuron_layer(x):
	#define connections
	#define rng weights for connections
	
def output_layer(y):
	#classify
	
def finished():
	#output a file with the completed network
	